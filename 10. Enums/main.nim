type
    Color = enum RED, GREEN, BLUE

var text_color = Color.RED

if text_color == Color.RED:
    echo "Text Color is Red"
elif text_color == Color.BLUE:
    echo "Text Color is Blue"
else:
    echo "Text Color is Green"